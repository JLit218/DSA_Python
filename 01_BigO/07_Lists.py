##################   lists   ###################
#.append or .popo do not change index
#when using .append it is O(1)
#inserting or removing from any index other than the tail causes indexes to change, so it is O(n)